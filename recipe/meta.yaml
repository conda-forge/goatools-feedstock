{% set name = "goatools" %}
{% set version = "1.4.4" %}
{% set sha256 = "599d3f84d7d9457b0c6c6ebbc09ffd68f273c4cca0c0ba7814a1a2d4c15632b9" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/goatools-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  noarch: python
  script_env:
    - SETUPTOOLS_SCM_PRETEND_VERSION={{ version }}
  script: {{ PYTHON }} -m pip install . --no-deps --no-build-isolation -vvv

requirements:
  host:
    - python >=3.7
    - pip
    - pytest
    - setuptools-scm
  run:
    - python >=3.7
    - setuptools
    - pandas
    - numpy
    - scipy
    - statsmodels
    - openpyxl
    - docopt
    - pydot
    - requests
    - rich
    - xlsxwriter

test:
  imports:
    - goatools
    - goatools.obo_parser
    - goatools.go_enrichment
    - goatools.associations
  commands:
    - find_enrichment.py -h
    - map_to_slim.py -h
    - plot_go_term.py -h

about:
  home: https://github.com/tanghaibao/goatools
  summary: Python library to handle and find enriched Gene Ontology (GO) terms
  license: BSD-2-Clause
  license_family: BSD
  license_file: LICENSE
  doc_url: https://pypi.org/project/goatools/

extra:
  recipe-maintainers:
    - mencian
